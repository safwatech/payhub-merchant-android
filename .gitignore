*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# `gradle/wrapper/gradle-wrapper.jar` is intentionally tracked — without it
# `./gradlew` can't bootstrap, which breaks every fresh clone + CI runner.

# Secrets — never commit
app/google-services.json
keystore.properties
*.jks
*.keystore
release.keystore
